export interface video{
    title: string;
    description: string;
    hlsURL: string;
    thumbnailPath: string;
    uploadDate: Date;
    duration: number;
    uploader: string;
    views: number;
}